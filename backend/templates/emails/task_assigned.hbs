<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Task Assigned</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }
        .content {
            padding: 30px;
        }
        .task-card {
            background: #f0f9ff;
            border-left: 4px solid #10b981;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .task-title {
            font-size: 20px;
            color: #065f46;
            margin: 0 0 10px 0;
        }
        .task-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin: 15px 0;
        }
        .detail-item {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .detail-label {
            font-weight: 600;
            color: #555;
            min-width: 100px;
        }
        .detail-value {
            color: #333;
        }
        .priority {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }
        .priority-high {
            background: #fee2e2;
            color: #dc2626;
        }
        .priority-medium {
            background: #fef3c7;
            color: #d97706;
        }
        .priority-low {
            background: #d1fae5;
            color: #065f46;
        }
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 25px;
            font-weight: 600;
            margin: 20px 0;
        }
        .footer {
            background-color: #f9f9f9;
            padding: 20px;
            text-align: center;
            color: #777;
            font-size: 12px;
            border-top: 1px solid #eee;
        }
        .assigner {
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 20px 0;
            padding: 15px;
            background: #f8fafc;
            border-radius: 8px;
        }
        .assigner-avatar {
            width: 40px;
            height: 40px;
            background: #10b981;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }
        @media (max-width: 600px) {
            .container {
                margin: 10px;
                border-radius: 0;
            }
            .content {
                padding: 20px;
            }
            .task-details {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎯 New Task Assigned</h1>
        </div>
        
        <div class="content">
            <p>Hello <strong>{{assigneeName}}</strong>,</p>
            
            <p>You have been assigned a new task. Here are the details:</p>
            
            <div class="task-card">
                <h2 class="task-title">{{taskTitle}}</h2>
                
                <div class="task-details">
                    <div class="detail-item">
                        <span class="detail-label">Project:</span>
                        <span class="detail-value">{{projectName}}</span>
                    </div>
                    
                    <div class="detail-item">
                        <span class="detail-label">Priority:</span>
                        <span class="priority priority-{{priority}}">{{priority}}</span>
                    </div>
                    
                    <div class="detail-item">
                        <span class="detail-label">Due Date:</span>
                        <span class="detail-value">{{dueDate}}</span>
                    </div>
                    
                    <div class="detail-item">
                        <span class="detail-label">Estimated Time:</span>
                        <span class="detail-value">{{estimatedHours}} hours</span>
                    </div>
                </div>
                
                {{#if taskDescription}}
                <div style="margin: 15px 0;">
                    <strong>Description:</strong>
                    <p style="color: #555; margin: 8px 0 0 0;">{{taskDescription}}</p>
                </div>
                {{/if}}
                
                {{#if tags.length}}
                <div style="margin: 15px 0;">
                    <strong>Tags:</strong>
                    <div style="display: flex; gap: 8px; margin-top: 8px; flex-wrap: wrap;">
                        {{#each tags}}
                        <span style="background: #e0f2fe; color: #0369a1; padding: 4px 10px; border-radius: 15px; font-size: 12px;">
                            {{this}}
                        </span>
                        {{/each}}
                    </div>
                </div>
                {{/if}}
            </div>
            
            {{#if assignerName}}
            <div class="assigner">
                <div class="assigner-avatar">{{assignerInitials}}</div>
                <div>
                    <div><strong>Assigned by:</strong> {{assignerName}}</div>
                    <div style="color: #666; font-size: 14px;">{{assignerEmail}}</div>
                </div>
            </div>
            {{/if}}
            
            <div style="text-align: center;">
                <a href="{{taskUrl}}" class="button">View Task Details</a>
            </div>
            
            <p style="color: #666; font-size: 14px;">
                You can update the task status, add comments, or upload files directly from the task page.
            </p>
            
            <p>
                Best regards,<br>
                <strong>Task Management System</strong>
            </p>
        </div>
        
        <div class="footer">
            <p>
                This is an automated notification from Task Management System.<br>
                <a href="{{unsubscribeUrl}}">Manage notification preferences</a>
            </p>
        </div>
    </div>
</body>
</html>